#include <bits/stdc++.h>
using namespace std;

int sum = 0;
vector<int> Add_array(vector<int> a)
{
  Add_array

}
int main()
{
  int n;
  cin >> n;
   vector<int> a(n);
   for (int i = 0; i < n; i++)
   cin >> a[i];

   vector<int>ans = Add_array(a);
  

  return 0;
}