#include <bits/stdc++.h>
using namespace std;
int main()
{
 int n=7;
 int low = 1, high = n;
 while (low <= high)
 {
   int mid = low + (high - low )/2;
   int ans=0;
   if(mid*mid <=n)
   {
    ans= tr;
    high = mid-1;
   }
   if ()
 }
 
  return 0;
}